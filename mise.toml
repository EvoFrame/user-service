[hooks]
enter = """
mise install --quiet
if [ -f .env.local ]; then
  export $(grep -v '^[[:space:]]*#' .env.local | grep '=' | sed 's/[[:space:]]*#[^=]*$//' | xargs)
fi
uv sync
"""

[tools]
python = "3.14"
uv = "latest"

[env]
_.file = [".env", ".env.local"]
APP_ENV = "development"
